#!/usr/bin/env bash

PROGRAM_NAME="$1"
shift 1

# Terminate already running bar instances
killall -q "$PROGRAM_NAME"

# Wait until the processes have been shut down
while pgrep -u $UID -x "$PROGRAM_NAME" >/dev/null; do sleep 1; done

$PROGRAM_NAME $@ &

