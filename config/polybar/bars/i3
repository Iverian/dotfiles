[global]
include-file = colors

include-file = modules/i3
include-file = modules/window
include-file = modules/volume
include-file = modules/battery
include-file = modules/keyboard
include-file = modules/wlan
include-file = modules/bluetooth
include-file = modules/date

[bar/i3]
monitor = ${env:MONITOR}
locale = ${env:LANG}

dpi = ${xrdb:dpi:96}
width = 100%
height = 35
fixed-center = true
bottom = false

border-left-size    = 10
border-right-size   = 10
border-bottom-size  = 6
border-top-size     = 6

border-top-color    = ${colors.bg}
border-bottom-color = ${colors.bg}
border-left-color   = ${colors.bg}
border-right-color  = ${colors.bg}
background          = ${colors.bg}
foreground          = ${colors.fg}

module-margin-left = 0
module-margin-right = 2

font-0 = Noto Sans Mono:size=9;3
font-1 = NotoSansMono Nerd Font:size=10;5

modules-left = i3
modules-center = window
modules-right = volume battery keyboard wlan bluetooth date

tray-position = right
tray-spacing = 2
tray-padding = 4
tray-maxsize = 35
tray-background = ${colors.bg}

cursor-click = pointer
cursor-scroll = ns-resize

; vim:ft=cfg
